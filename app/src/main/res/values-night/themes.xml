<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.Zmaj" parent="Theme.MaterialComponents.DayNight">
        <!-- Primary colors -->
        <item name="colorPrimary">@color/primaryColorDark</item>
        <item name="colorPrimaryVariant">@color/primaryColorLight</item>
        <item name="colorOnPrimary">@color/textDark</item>

        <!-- Background colors -->
        <item name="android:colorBackground">@color/backgroundDark</item>
        <item name="android:textColor">@color/textDark</item>
    </style>
</resources>
